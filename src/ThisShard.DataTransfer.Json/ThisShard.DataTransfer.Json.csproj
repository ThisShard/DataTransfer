<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <RootNamespace>ThisShard.Database.Infrastructure</RootNamespace>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Version>0.1.0</Version>
        <Title>ThisShard.DataTransfer.Json</Title>
        <Authors>ThisShard</Authors>
        <Description>Data transfer for Json</Description>
        <Copyright>ThisShard</Copyright>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\ThisShard.DataTransfer.Core\ThisShard.DataTransfer.Core.csproj" />
    </ItemGroup>

    <ItemGroup>
      <PackageReference Include="Utf8JsonAsyncStreamReader" Version="2.1.0" />
    </ItemGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
        <OutputPath>..\bin\Debug/net8.0/</OutputPath>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
        <OutputPath>..\bin\Release/net8.0/</OutputPath>
    </PropertyGroup>

</Project>
